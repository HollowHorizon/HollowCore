name: mavenPublish
on:
  push:
    branches: [ "stonecutter" ]
jobs:
  build:
    strategy:
      matrix:
        java: [21]
        os: [ubuntu-20.04]
    runs-on: ${{ matrix.os }}
    steps:
      - name: checkout repository
        uses: actions/checkout@v4
      - name: validate gradle wrapper
        uses: gradle/wrapper-validation-action@v1
      - name: setup jdk ${{ matrix.java }}
        uses: actions/setup-java@v1
        with:
          java-version: ${{ matrix.java }}
      - name: make gradle wrapper executable
        run: chmod +x ./gradlew
      - name: Release to Maven
        uses: gradle/gradle-build-action@v2
        if: |
          (!contains(github.event.head_commit, '[no-maven]') || github.event.inputs.no-maven != 'true') || (!contains(github.event.head_commit, '[no-actions]') || github.event.inputs.no-actions != 'true')
        env:
          MAVEN_USER: ${{ secrets.MAVEN_USER }}
          MAVEN_PASSWORD: ${{ secrets.MAVEN_PASSWORD }}
        with:
          arguments: chiseledPublishMaven
